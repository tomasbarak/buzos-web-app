<style>
    .top-nav-bar{
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: flex-start;
        font-family: 'Roboto', sans-serif; 
        height: 60px;
        padding: 5px 15px;
        box-sizing: border-box;
        border-bottom: 1px solid rgba(0, 0, 0, .25);
    }

    .top-nav-bar > .item{
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color:rgba(0, 0, 0, .75);
        width: auto;
        height: 100%;
        cursor: pointer;
        border-radius: 5px;
        transition: background-color .25s ease-in-out;
        background-color: transparent;
        box-sizing: border-box;
        padding: 10px;
        user-select: none;
    }
    .top-nav-bar > .item:hover{
        transition: background-color .25s ease-in-out;
        background-color: rgba(0, 0, 0, .05);
    }
    .top-nav-bar > .item + .item{
        margin-left: 15px;
    }
    .space{
        flex: 1;
    }
    #profile_photo{
        display: flex;
        width: 40px;
        height: 40px;
        border-radius: 50%;
        user-select: none;
        margin-left: 15px;
        animation: opacity_animation 1s ease-in infinite;
    }
    #profile_name{
        user-select: none;
    }

    @keyframes opacity_animation{
        0%{
            border: 2px solid rgba(0, 162, 255, .5);
        }
        50%{
            border: 2px solid rgba(0, 162, 255, 1);
        }
        100%{
            border: 2px solid rgba(0, 162, 255, .5);
        }
    }
</style>